VITE_SUPABASE_PROJECT_ID=your-project-ref
VITE_SUPABASE_ANON_KEY=your-publishable-anon-key

# Optional. Defaults to /StockManagement/ (GitHub Pages subpath). Set to /
# when deploying at the root of a custom domain -- see README's Deploy section.
# VITE_BASE_PATH=/
